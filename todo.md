# todo

* create md support, where we do like # text it creates a heading and update with a heading format

* we should be able to toggle between md format and normal format. 

* customizeble hot keys, bind your own hot keys

* export/import database

* run on os start

* add plugins support where example we make it possible to add a timer and push info to server if we have internet. 

